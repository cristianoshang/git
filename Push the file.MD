# How to push the file to github by git

1、首先将所在文件夹变为.git格式

```
$ git init
Initialized empty Git repository in C:/Users/****/Desktop/video-addLinklist/.git/
```

2、关联远程仓库
```
$ git remote add origin <url>
```
3、用命令行将文件夹中所有文件变为可以推送模式

```
$ git add *
```

4、确认添加所有文件

```
$ git commit -m "add all file"
```

5、远程推送

```
$ git push origin master
```

备注：（https://www.huaweicloud.com/articles/b56ddb05aad92e077813c643cd605dea.html）
